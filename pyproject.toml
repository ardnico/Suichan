[project]
name = "suichan"
version = "0.1.0"
description = "Mount Suichan event recording system"
authors = [{ name = "OpenAI Assistant" }]
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
